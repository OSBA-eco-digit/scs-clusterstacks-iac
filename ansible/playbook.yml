---
# Apply the 'scs-cluster-stack' role, which applies 'k3s' as dependency already
- hosts: all
  roles:
    - scs-cluster-stack
  vars:
    k3s_become: true
    k3s_release_version: "v1.33.7+k3s1"
    k3s_use_experimental: true
