# {{ ansible_managed }}
---
apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: {{ cluster_name }}
  namespace: {{ cluster_namespace }}
  labels:
    managed-secret: cloud-config
spec:
  clusterNetwork:
    pods:
      cidrBlocks:
        - {{ cluster_cidr_pods }}
    serviceDomain: cluster.local
    services:
      cidrBlocks:
        - {{ cluster_cidr_services }}
  topology:
    variables:
      - name: controller_flavor
        value: "{{ openstack_controlplane_flavor }}"
      - name: worker_flavor
        value: "{{ openstack_worker_flavor }}"
      - name: worker_root_disk
        value: "{{ openstack_worker_rootdisk_size }}"
      - name: external_id
        value: "{{ openstack_external_network_id }}"
    class: "{{ cluster_class }} | replace(".", "-")"
    controlPlane:
      replicas: 1
    version: v{{ cluster_release }}
    workers:
      machineDeployments:
        - class: default-worker
          failureDomain: nova
          name: default-worker
          replicas: 3
