---
- name: Update apt cache
  become: true
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install dependencies
  become: true
  apt:
    name: "{{ d }}"
    state: present
  loop: "{{ scs_cs_dependencies }}"
  loop_control:
    loop_var: d
